# T025 feedback slots: claim & release
> st:done@260908 | ssot:FDBK-1 FDBK-2 PROJ-4 ARCH-35 ARCH-38 | base:FDBK@2 PROJ@2 ARCH@2 | dep:T023 T012

## goal
Pressing Start holds one slot for 24 hours, with automatic release and the one-slot-per-account-per-mission rule.

## acceptance
- [v] `apps/api/src/feedback/` created with schema
      `feedback_claims(id uuid pk, mission_id uuid not null, user_id uuid not null, state text not null, held_until timestamptz not null, released_at timestamptz, created_at, updated_at, index(mission_id), unique(mission_id, user_id) where state in ('held','submitted','settled'))`
- [v] `POST /api/v1/missions/:id/claims` holds a slot for 24h (FDBK-1) when: signed in, mission open, caller is not the project owner, caller has no live claim on this mission, a claimable slot remains (FDBK-2)
- [v] rejections carry distinct codes: `MISSION_NOT_OPEN` `OWN_PROJECT` `ALREADY_CLAIMED` `NO_SLOTS_AVAILABLE`
- [v] `DELETE /api/v1/claims/:id` lets the holder give the slot back before submitting
- [v] `slot.release` job scheduled at `held_until` releases a still-held claim (state `released`), freeing the slot; cancelled on submit or manual release; re-running it is a no-op
- [v] a released claim does not block re-claiming — no re-claim limit in v1 (FDBK-1)
- [v] `ClaimHeld` / `ClaimReleased` events emitted for mission slot accounting (T023)
- [v] application service `countLiveClaims(missionIds)` used by T023/T024 for claimable-slot math, batched
- [v] test: unit tests for every rejection branch with in-memory repositories
- [v] test (int): the partial unique index blocks a second live claim by the same user while allowing a new claim after release; two concurrent claims for the last slot leave exactly one held
- [v] test (int): the release job frees the slot at `held_until`, is idempotent, and is cancelled by a submit

## impl notes
- one claim row per hold (rather than N pre-created slot rows per mission) ← slots are a count on the mission; materializing them would double the state to keep consistent, and the partial unique index already enforces FDBK-2
- `held_until` is absolute (opened at + 24h) and the job is scheduled with `runAt` (ARCH-35 note on clock drift)
- the "claimable" number is derived: `mission.slots − count(state in held|submitted|settled)`; nothing caches it
- the owner check reads the project through the project context's application service, not a join (ARCH-14)

## result
- `apps/api/src/feedback/`+ — `FeedbackClaim` aggregate with `ClaimHeld`/`ClaimReleased`, `claim-errors.ts`, `claim.repository.ts`, `claim-slot.use-case.ts`, the Drizzle repository, `release-slot.job.ts`, `claims.controller.ts`, `feedback.module.ts`
- migration `0007_*`+ — `feedback_claims` with the partial unique index that is FDBK-2 itself: one live claim per account per mission, while a released one leaves the way clear
- `shared/application/slot-occupancy.ts`+ — the occupancy contract moved out of `project/domain`, because `feedback` owns the rows and `project` asks the question; `mission-access.ts`+ is the mirror, what `feedback` may know about a mission
- `project/infrastructure/mission-access-adapter.ts`+ answers it; `NoSlotOccupancy`- and `NoActiveMissions`- are both gone, replaced by the real thing
- `@repo/contracts`✎ gained `feedback` with the claim shape
- tests: `claim-slot.use-case.test.ts` (20, every rejection branch), `claims.int.test.ts` (16 including two people racing the last slot)
- verified: `turbo run test lint typecheck build` 19/19 · domain+application coverage 86.7% lines · `depcruise` clean · `pnpm test:docker` green · contract regenerated with both claim paths
- the race is settled by `pg_advisory_xact_lock` on the mission id, not by `select … for update`: a mission with no claims yet locks no rows, so two people taking the only slot would both count zero. The advisory lock serialises them whether or not a row exists, and it is taken on the id rather than on the mission row because that row belongs to another context
- the project context's integration tests now run against the real claim store rather than a stub, and the held-slot case in `missions.int.test.ts` is a real row. `ClaimStoreModule` is global because `feedback` already imports `project` to read a mission, so `project` cannot import back without a cycle
- error codes are exactly the ones the acceptance names — `MISSION_NOT_OPEN`, `OWN_PROJECT`, `ALREADY_CLAIMED`, `NO_SLOTS_AVAILABLE` — each already subject-prefixed in substance
- `ClaimHeld` and `ClaimReleased` are emitted and nothing subscribes yet. The slot arithmetic derives its numbers rather than following the events, so they exist for T030's notifications; the names are registered as cross-context so that stays a wiring change
- with real claims now countable, PROJ-9's ranking note from T024 becomes live: a mission whose slots are all held still ranks ahead of one without a mission, because the rank asks "has an open mission" rather than "has a takeable slot". The displayed count is right; only the ordering is coarse. A held-slot counter on the mission row would fix it and is a create-task decision
- not divergent from SSOT beyond that note
