# T027 feedback settle: accept, reject, auto-accept
> st:done@260908 | ssot:FDBK-6 FDBK-7 CRED-4 PROJ-6 ARCH-35 ARCH-38 ARCH-39 | base:FDBK@2 CRED@2 PROJ@2 ARCH@2 | dep:T026 T021 T012

## goal
The maker's accept/reject decision, the 48h warning and 72h auto-accept timers, and the credit movements each produces.

## acceptance
- [v] `POST /api/v1/feedbacks/:id/accept` (project owner only, state `pending`) → state `accepted`, 1 credit moves from mission escrow to the feedbacker (CRED-4), `SlotSettled` emitted
- [v] `POST /api/v1/feedbacks/:id/reject` requires a reason from the fixed set `task_not_done` `no_substance` `spam_abuse` plus an optional note; → state `rejected`, 1 credit returns from escrow to the maker's balance (FDBK-6, CRED-4); reason and note are visible to the feedbacker and public with the report (FDBK-9)
- [v] `feedback.auto-accept` job at +72h settles a still-pending report exactly as an accept and marks it auto-accepted, distinguishable in the response (FDBK-7)
- [v] `feedback.warn` job at +48h emits `AutoAcceptWarning { feedbackId, makerId }` for NOTI (T030) only when the report is still pending
- [v] settling cancels the remaining timers and is idempotent: a second settle attempt on a non-pending report returns 409 `{code:"FEEDBACK_ALREADY_SETTLED"}` and moves no credits
- [v] when the settled slot was the mission's last outstanding one, `SlotSettled` drives the mission to `completed` (T023)
- [v] the whole settle runs in one transaction across feedback + credit + mission state (ARCH-38); the notification effects are jobs enqueued in it (ARCH-39)
- [v] test: unit tests — non-owner rejected, unknown reason rejected, note optional, double settle blocked, exact credit calls asserted per branch
- [v] test (int): accept moves exactly 1 credit escrow→feedbacker and the ledger invariant still holds; reject moves it escrow→maker
- [v] test (int): the auto-accept job at 72h pays the feedbacker and is a no-op when the report was already settled; running it twice pays once
- [v] test (int): the warn job fires only for still-pending reports; settling before 48h cancels both jobs

## impl notes
- auto-accept reuses the accept use case with an `actor: 'system'` flag rather than a parallel code path ← the credit movement must be provably identical
- idempotence is enforced by the state check inside the transaction plus the credit ledger's `(type, account, ref)` unique key, so even a duplicated job cannot double-pay
- rejection reasons are a domain enum shared through `packages/contracts` so the web radio group and the profile stats (T029) use one list
- the maker cannot reject after 72h — the auto-accept has already settled it; the 409 above is the honest answer rather than a silent no-op

## result
- `feedback/application/settle-feedback.use-case.ts`+ — accept, reject, autoAccept and warn, all four through one `settle` so the credit movement has no second place it could differ
- `feedback/domain/feedback.ts`✎ gained `SlotSettled` and `AutoAcceptWarning`, an `automatic` flag and the `warnMaker` recording; `accept`/`reject` now take who settled it and record the event
- `feedback/infrastructure/settlement-timers.job.ts`✎ — the two placeholders T026 registered now have bodies
- `feedback/presentation/feedbacks.controller.ts`✎ — `POST /feedbacks/:id/accept` and `/reject`, both 200 because a decision creates nothing
- migration `0009_*`+ adds `feedbacks.automatic`; `@repo/contracts`✎ gained the reject request and `automatic`
- tests: `settle-feedback.use-case.test.ts` (23, exact credit calls per branch), `settle.int.test.ts` (14 against a real ledger, invariant re-checked after every settlement)
- verified: `turbo run test lint typecheck build` 19/19 · domain+application coverage above the 80 gate · `depcruise` clean · `pnpm test:docker` green · contract regenerated with both settle paths
- **fixed a real gap the tests found**: `DrizzleMissionRepository`, `DrizzleClaimRepository` and `DrizzleFeedbackRepository` never drained their aggregates' events into the collector, so nothing they recorded ever reached the bus. T023's `MissionEnded` had been silently going nowhere since it was written, and the mission would never have completed. All three now drain on save, the way the user repository already did
- the rejection reasons are `task_not_done` `no_substance` `spam_abuse`, as this task names them. T026 had guessed the hyphenated form with nothing to go on; nothing consumed it yet, so it was corrected rather than kept
- `FEEDBACK_ALREADY_SETTLED` is the code the acceptance asks for, so `FeedbackNotPendingError` carries it; the separate "this slot already has a report" case became `FEEDBACK_ALREADY_SUBMITTED`, because the two are different things and one code cannot honestly mean both
- auto-accept reuses the accept path with no actor rather than a parallel one, and is idempotent twice over: the state check inside the transaction, and the ledger's own `(type, account, ref)` key, so even a duplicated job pays once. The integration test runs it twice and checks the balance
- not divergent from SSOT
