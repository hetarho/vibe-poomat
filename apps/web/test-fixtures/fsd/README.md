Deliberately illegal FSD slices. `fsd.test.ts` lints this tree with the recommended
ruleset and asserts both violations are reported, so a rule that stops working
cannot pass unnoticed. Nothing here is compiled or shipped.
